name: Hello

on: [push]

jobs:
  greet:

    runs-on: ubuntu-latest
    
    steps:
      - id: hello_world
        name: Hello World
        uses: actions/action@master
        with:
          who-to-greet: 'jack'
