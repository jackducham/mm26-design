name: Hello

on: [push]

jobs:
  greet:

    runs-on: ubuntu-latest
    
    steps:
      - id: hello_world
        name: Hello World
        uses: jackducham/mm26-design/.github/actions/action
        with:
          who-to-greet: 'jack'
